# 2026-03-28

## Basis Docs Session (Diamond, BASIS PRODUCTION group)

### Completed
- Created `INDEX_DESCRIPTIONS.md` — source-of-truth for section descriptions used to compile INDEX files
- Rebuilt `INDEX_V3.md` from descriptions file with correct V3 links and cross-refs
- Built `build_docs.py` — full build pipeline (INDEX, COMPLETE, COMPLETE_INDEX, zip with versioned/ + production/ folders)
- Built `add_module.py` — insert (with renumbering) or append new modules
- Tested both scripts successfully on V3 docs
- Reviewed launchonbasis.com for AI discovery: meta tags ✅, llms.txt ✅, robots.txt added ✅, .well-known/agent.json referenced
- Suggested agent.json structure for Alex
- Discussed ERC-8004 agent verification (inverse CAPTCHA problem — no clean solution, ACS over time is the real filter)

### Discussions
- **The Reef**: Moving toward being the umbrella social layer (feed + leaderboards + profiles + ACS). Replaces/absorbs "Moltbook" concept. Structure still being refined:
  - Feed: Everyone / Humans / Agents sections
  - Leaderboard: Balance (all users), Points (rank only, no exact numbers, all users), ACS (agent-only)
  - Profiles: ACS, tier badge, history — discoverable by anyone
- **Moltbook**: Confirmed by Diamond that The Reef IS the Moltbook on Basis. Need to consolidate naming in docs.
- Diamond shared early profile page screenshot (Alex building it)

### To-Do (Basis Docs — pending Alex input)
- [ ] **The Reef module** — finalize structure and content before creating new doc module. Needs Alex input on API endpoints, gating thresholds, profile features.
- [ ] **MCP module** — local server version built and working. Needs GitHub repo setup and README updated to match final build spec.

### Technical Notes
- Windows PowerShell needs `$env:PYTHONIOENCODING="utf-8"` before running build scripts (emoji output issue, file writing is fine)
- build_docs.py and add_module.py saved in `projects/basis/basis-docs/`
