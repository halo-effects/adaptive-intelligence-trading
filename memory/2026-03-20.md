# 2026-03-20 (Friday)

## Session 1 — Morning heartbeat + V14PM equity bug fix (07:01–07:15)

### V14PM Live Equity Bug (FIXED, committed)
- **Dashboard bug**: Capital utilization donut showed 100% instead of ~7.7%. Cause: `S.cash` missing from V14PM status.json — cash lives in `router.active_cash` + `router.reserve_cash`. Dashboard only checked `S.cash`.
- **Engine bug (root cause)**: `fetch_balance()` returned `USDT.free` (available margin only, excludes locked margin). V14 Live bot correctly uses `usdt_total`. Result: equity reported as $102.94 instead of ~$350.
  - Position value calc also wrong: added full notional (`entry_price * qty`) on top of free USDT — double-counting.
- **Comparison with V14 Live**: V14 Live uses `executor.get_balance()` → `usdt_total + base_total * price`. V14PM was using `USDT.free + entry*qty + unrealized`.
- **Fixes applied**:
  - Added `fetch_full_balance()` returning `{usdt_free, usdt_total}`
  - `_compute_equity()` and `_write_status()` now use `usdt_total + unrealized_pnl`
  - Added `cash` and `exchange_balance` fields to status.json
  - Dashboard donut falls back to `router.active_cash + router.reserve_cash`
- **Commit**: `812d5264` on main
- **Bot needs restart** to pick up engine fix

### Bot Status (07:01 check)
- V14PM Live: Running, GRASS/USDT LONG_DCA, 2 layers, ~$27 invested, $324 cash. 13 deals, 100% WR, $24.40 realized PnL.
- V14 Live (Legacy): **Status stale** — last update 2026-03-19T14:51 (~23.5h old). ASTER/USDT, +2.94%.
- V14 Paper: Running, +426.34% ($52.6K equity). All 4 coins in LONG_DCA.
- V14 PM Paper: Running, +13.71% ($56.8K equity). 13 symbols, 4 with open positions (ZRO, PENDLE, SNX, HYPE).
- Cron consolidation: OK (ran 02:00 PST)

### Notes
- Telegram connection may be broken — Brett restarting gateway.
- V14 Live Legacy bot status stale >23h — may need investigation after gateway restart.

---

## Session 2 — Basis Production (all day)

### Major: Unified Docs Created
- **`docs-unified.md`** — 37KB, 11-part "Complete Agent Guide" created with Diamond
- Merged knowledge base + all 5 existing doc drafts into single source of truth
- Diamond provided key corrections:
  - Leverage = recursive loan stacking, NOT flat 2% fee
  - $50 minimum was internal only, not general guidance
  - 100% LTV on Stable+ (spot=floor), less on Floor+ (floor < spot)
- Remaining gaps to fill: ACS scoring details, Moltbook features, mixedBuy(), resolution mechanics

### Major: Full Contract Redeployment (Alex)
- **All 13 contracts redeployed** with new addresses
- **15 event changes**: 4 fixes + 11 new events
- New Prisma models: Loan, LoanEvent, VaultEvent, VestingEvent
- Sync API: `POST /api/v1/sync` (auto-detects source, idempotent)
- New read endpoints: `/loans`, `/loans/events`, `/vault/events`, `/vesting/events`
- Updated Python + JS SDKs delivered with correct addresses
- Dapp + API updated to new contracts

### SDK Validation
- Smoke test on new deployment: 7/8 PASS (all reads working)
- One bug: `get_constants` resolver signature mismatch
- Both SDKs extracted, verified contract addresses match dapp (all 13)
- Previous SDKs archived to `*-old/` directories

### Project Files Updated
- `projects/basis/index.md` — v6 (unified docs, redeployment, sync API, contract addresses, SDK packages)
- `projects/basis/docs-unified.md` — NEW (the complete agent guide)
- `projects/basis/sdk-docs-2026-03-20.md` — NEW (v3 SDK docs post-redeployment)
- `projects/basis/standup-2026-03-21.md` — NEW (tomorrow's priorities)
- `projects/basis/standup-2026-03-20.md` — today's standup

### Pending for Tomorrow
- Write tests on new contracts (need faucet USDB — wallet is 0)
- Diamond continues docs review for more corrections
- Points system decisions needed: vesting values, anti-gaming strategy
- SDK publish decision (npm/PyPI or zip)
