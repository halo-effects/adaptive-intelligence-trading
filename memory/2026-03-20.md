# 2026-03-20 (Friday)

## Session 1 — Morning heartbeat + V14PM equity bug fix (07:01–07:15)

### V14PM Live Equity Bug (FIXED, committed)
- **Dashboard bug**: Capital utilization donut showed 100% instead of ~7.7%. Cause: `S.cash` missing from V14PM status.json — cash lives in `router.active_cash` + `router.reserve_cash`. Dashboard only checked `S.cash`.
- **Engine bug (root cause)**: `fetch_balance()` returned `USDT.free` (available margin only, excludes locked margin). V14 Live bot correctly uses `usdt_total`. Result: equity reported as $102.94 instead of ~$350.
  - Position value calc also wrong: added full notional (`entry_price * qty`) on top of free USDT — double-counting.
- **Comparison with V14 Live**: V14 Live uses `executor.get_balance()` → `usdt_total + base_total * price`. V14PM was using `USDT.free + entry*qty + unrealized`.
- **Fixes applied**:
  - Added `fetch_full_balance()` returning `{usdt_free, usdt_total}`
  - `_compute_equity()` and `_write_status()` now use `usdt_total + unrealized_pnl`
  - Added `cash` and `exchange_balance` fields to status.json
  - Dashboard donut falls back to `router.active_cash + router.reserve_cash`
- **Commit**: `812d5264` on main
- **Bot needs restart** to pick up engine fix

### Bot Status (07:01 check)
- V14PM Live: Running, GRASS/USDT LONG_DCA, 2 layers, ~$27 invested, $324 cash. 13 deals, 100% WR, $24.40 realized PnL.
- V14 Live (Legacy): **Status stale** — last update 2026-03-19T14:51 (~23.5h old). ASTER/USDT, +2.94%.
- V14 Paper: Running, +426.34% ($52.6K equity). All 4 coins in LONG_DCA.
- V14 PM Paper: Running, +13.71% ($56.8K equity). 13 symbols, 4 with open positions (ZRO, PENDLE, SNX, HYPE).
- Cron consolidation: OK (ran 02:00 PST)

### Notes
- Telegram connection may be broken — Brett restarting gateway.
- V14 Live Legacy bot status stale >23h — may need investigation after gateway restart.
