## Aster Migration — Bot Runners (5:28 AM - 5:35 AM)

### Problem
V14PM logging `Failed to fetch candles for ORCA/USDT (ORCA/USDC:USDC): hyperliquid does not have market symbol ORCA/USDC:USDC`. Scanner picks Aster-only coins (ORCA, TRUMP, BERA, etc.) but bot runners were still fetching realtime candles from Hyperliquid.

### Root Cause
The Aster migration on April 17-18 only covered `collect_scanner_candles.py`. The three paper bot runners still used `ccxt.hyperliquid()` with `{COIN}/USDC:USDC` symbols for their live candle loops. Same class of bug as the collector revert — incomplete migration.

### Fix
Migrated all three bot runners to Aster:
- **run_v14_portfolio_paper.py**: `ccxt.hyperliquid()` → `ccxt.aster()`, added `_to_aster_sym()` helper with 1000-prefix price scaling (PEPE/BONK/FLOKI divide by 1000)
- **run_v14_paper.py**: `HL_PRICE_MAP` → `ASTER_PRICE_MAP` (USDC:USDC → USDT:USDT)
- **run_v14etf_paper.py**: Same map migration

All three paper bots restarted via scheduled tasks. Confirmed ORCA/USDT candles now fetching successfully from Aster.

Live bot (`run_v14_live_aster.py`) already uses `SpotExchangeClient` — no change needed.

### Backup Alert
3:01 AM backup failed: `EBUSY: resource busy or locked, read` (exit code 1). Likely file lock conflict. May resolve on retry.

### Full Pipeline Audit (5:38 AM)
End-to-end check of scanner → coin selection → bot execution → dashboard:

1. **Scanner** (`v14_cycle_scanner.py`): Reads from `candles.db` — exchange-agnostic ✅
2. **Capital Router** (`v14_capital_manager.py`): Reads `cycle_scanner.json` — exchange-agnostic ✅
3. **Bot Runners**: All 3 now on Aster ✅
4. **Dashboards**: Read from `status.json` + `trades.csv` — no exchange dependency ✅
5. **Dashboard Sync** (`sync_dashboard.ps1`): Copies local files to GitHub Pages — no exchange refs ✅
6. **Aster symbol coverage**: 44/45 scanner coins exist on Aster. Only W (Wormhole) missing — graceful skip.
7. **Scanner-Collector mismatch**: 11 scanner coins NOT in collector (COMP, DYDX, ENS, KAS, LDO, MKR, OP, RUNE, TON, W, ASTER). Some have stale candle data (March 9). Not causing harm since scores are 0 / below hurdle.
8. **V14-ETF**: Had to Stop-ScheduledTask (old PID 8304 from Apr 14 was access-denied). Restarted clean.
9. **--exchange CLI default**: Updated from "hyperliquid" to "aster" in all 3 runners (metadata only). Scheduled tasks still pass `--exchange hyperliquid` explicitly — needs elevated access to update.
10. **Trade data safety**: All bots restored state correctly — V14 (464 trades), V14-ETF (55 trades), V14PM (23 engines, 9 open deals, $71K equity). Nothing lost.

### Unified Aster Coin Universe (3:22 PM)
Brett asked to only include coins that exist on Aster. Audited both scanner and collector:
- **Removed**: W (Wormhole) — not on Aster perps
- **Added to scanner** (15 coins): APT, BERA, BONK, FLOKI, GRASS, INIT, IP, JTO, MOVE, ORCA, PEPE, S, TRUMP, VIRTUAL, ZEC
- **Added to collector** (10 coins): ASTER, COMP, DYDX, ENS, KAS, LDO, MKR, OP, RUNE, TON
- **Normalized** scanner symbols from USDC to USDT (BTC, ETH, SOL, HYPE)
- **Updated** taker fee from 0.00025 (Hyperliquid) to 0.00035 (Aster)
- **Result**: Both files now have identical 60-coin Aster-only universe
- **Ran collector**: All 60 coins up to date, 0 failures
- **Updated HEARTBEAT.md**: All Hyperliquid refs → Aster, 45 → 60 coins

### Commits
- `20d689e6b` — "Migrate all bot runners from Hyperliquid to Aster for realtime candle data"
- `79f1c62d7` — "Update --exchange default from hyperliquid to aster (metadata only)"
- `0e8d6573f` — "Unify scanner + collector to 60-coin Aster-only universe"
- `5f281d0cb` — "Update HEARTBEAT.md: Hyperliquid refs to Aster, 60-coin universe"
