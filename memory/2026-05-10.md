# 2026-05-10 — V2 System Audit Complete + Production Fixes

## V2 System Audit (Phases 1-11)
- 60 findings across 11 phases, ~15,000 lines reviewed
- 2 CRITICAL + 1 HIGH fixed during audit
- 10 more fixed post-audit (Fix Now + Quick Wins batch)
- Full summary: `projects/ait/specs/v2-audit-summary.md`

## Fix Now Batch (6 items)
- #12: Steve 3-Check symbol selection → uses best symbol (indicators + widest range)
- #13: HybridDetector2D USDC preference → same scoring as load_daily()
- #21: Scanner freshness warning → Telegram alert if >24h old
- #24: Candle quality validation → filters bad rows before DCA simulation
- #49: Old V14LiveAster task stale → needs admin to disable
- #51: Stale lock recovery → PID check, auto-cleanup dead process locks

## Quick Wins Batch (4 items)
- #20: Hurdle rate → `HURDLE_RATE_DCA_SCORE = 5.0` constant in v14_capital_manager.py
- #27: Per-coin PnL → in-memory trade list (was reading CSV from disk every 60s)
- #43: Phantom open_deals → cleaned during exchange sync
- #38: Capital ledger → documented as stale/informational only

## Critical Production Fixes

### Dashboard Sync Root Cause
- `sync_dashboard.ps1` used `git reset --soft origin/main` in sparse-checkout temp repo
- Soft reset populated index with FULL tree from remote (paper/, trading/, memory/)
- Non-docs files silently committed/deleted in feedback loop
- This is what truncated trade CSVs → alternating 84/600+ trade pattern in git history
- Fix: fresh shallow clone per cycle, belt-and-suspenders non-docs check
- Added Hard Rule #28

### Trade History Restoration
- Paper: 671 historical + 79 new = 750 trades, $50,415 PnL (was 171/$14K)
- Live: 82 historical + 4 new = 86 trades, $13.96 PnL (was 77)
- Dedup by (symbol, open_time, close_time), deal IDs renumbered
- Added Hard Rule #29 (append-only CSV)

### seed_capital Drift
- DEX-as-truth startup was computing: `seed = balance - csv_pnl - unrealized`
- With incomplete CSV, this derived $364 instead of $300
- Equity chart started too high, showed phantom drops
- Fix: seed_capital = CLI --capital, set once in __init__, never recalculated
- Added Hard Rules #26 and #27

## Still Needs Admin
1. Create V14PM Live auto-restart task (`Register-ScheduledTask -TaskName "V14PMLiveAster"`)
2. Disable old stale task (`schtasks /Change /TN "V14LiveAster" /DISABLE`)

## Commits
- `c105238b1` — fix: 5 audit findings (symbol selection, freshness, validation, lock)
- `0d1f0b955` — fix: 4 more audit findings (hurdle rate, PnL cache, phantom deals)
- `62bae4c71` — fix: dashboard sync root cause + trade history restoration
- `07fe36cdb` — fix: seed_capital drift + dashboard sync (production)
