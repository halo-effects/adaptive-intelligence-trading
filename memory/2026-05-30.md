# 2026-05-30 — Saturday

## Post-TP Rotation Zero-Allocation Bug Fix
- **Bug**: First production rotation — INJ/USDT (score 0.0) TP'd, HYPE/USDC (score 57.4) rotated in with Capital: $0.00
- HYPE engine ticked hourly with `no action (warmed_up=True)` — DCA sizing computed $0 buy size
- $160 active cash sat idle, 3/3 slots occupied (TON, JUP, PENDLE with positions)
- **Root cause**: `_rotate_after_tp()` inherited incumbent's allocation via `active_allocations.pop()`. INJ score decayed to 0 → rebalance gave $0 → HYPE inherited $0
- **Fix**: Check `alloc <= 0` after pop, derive fair share: `active_pool_total / tier_coin_cap`. Paper runner already had this (line 724). Live runner was missing it.
- Also fixed liquidity filter volume reference to use fair-share fallback
- Pre-flight passed. Commit `77989855a`, pushed.
- **Docs updated**: spec §9 (bug fix section), architecture §7.2 (rotation docs), project log (05-29 + 05-30 entries), overview (current state), MEMORY.md
- **Pending**: Bot needs restart to pick up the fix + state.json needs HYPE allocation patched

## Morning Briefing (07:00 PDT)
- V14PM Live: RUNNING, $430 capital, $343 equity, 3 open positions (TON/JUP/PENDLE all L1, all deep underwater)
  - Total unrealized: -$88.06
  - Realized: +$25.33 (112 deals, 84.8% WR)
  - Cash: $124 (active $160, reserve $43)
  - CFGI: 37.3 (fear), regime LONG_DCA, trend bullish
  - HYPE/USDC rotated in with $0 allocation — zero-alloc bug still active, fix pending restart
  - Multiple non-approved coins (TAO, JTO, PEPE, DYDX, ENA) correctly blocked by T1 gate
- Overnight: Deal #111 INJ/USDT closed +$1.65 (3.11%), 3 layers, 12.8h hold at 4:48 AM
- V14 Paper: RUNNING, $40,707 equity (+307%), NEAR short L10 past liquidation (-8.6% — slightly improved)
- V14PM Paper: status.json still missing
- All crons healthy
- **Still blocked**: Zero-alloc fix needs bot restart + state.json HYPE patch
