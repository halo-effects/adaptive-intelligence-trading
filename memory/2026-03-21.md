# 2026-03-21 — Saturday Session Notes

## Basis SDK Docs — Major Session (~3 hours, Diamond + Alex + Brett)

### What Happened
Marathon session improving Basis SDK documentation from 6.5/10 to 7/10 (technical) with a "would build tonight" result on the action-oriented test. Ran 5 fresh agent reviews (v2–v6), each time fixing issues found, re-running, and iterating.

### Key Discoveries
- **hybridMultiplier=100 = Stable+** — confirmed by Alex from actual Solidity source code
- **Contract-enforced limits**: hybrid 1-100, startLP 100-10K, usdbForBonding 0-150K (must be ≥1 if frozen=true)
- **startLP is virtual (free)** — costs creator nothing, just sets dollar scale of price movement
- **hybridMultiplier ≈ % of slippage retained** — empirically tested on-chain with real tokens
- **Created 5 test tokens on BSC** at multipliers 15/30/45/60/90, ran buy/sell cycles to measure price impact
- **Price impact formula**: hybrid=N means price moves ~(100-N)% as much as hybrid=1 per trade
- **Floor retention**: higher hybrid = more floor gained per buy→sell cycle

### On-Chain Testing Results
Tokens created from wallet `0x2D087a119271Ef50C72eC5F01183f85Ab7E74Fe2`:
- TH15: `0xf300e97967bddeeaf93514e86a6a16c787325250`
- TH30: `0x7f6c6a5e0fa11230653ca938913a9bcfeb8ea4ce`
- TH45: `0xa95b0db97d97e74c7cf298b1ebfe31d1699cef8c`
- TH60: `0x4d5ce0768f3bd2df239685ef236d7629ef779830`
- TH90: `0xda684cdf8248eae2b875a8c9bc2e968ffbc478a7`

Note: Created via `createToken()` not `createTokenWithMetadata()` — they don't show on dapp (no metadata registered). Tokens exist on-chain only.

### Doc Changes Applied (batch)
1. autoVest = creator-bought tokens (NOT pre-minted)
2. Points strategy rewrite — vague signals only, no values or cap hints
3. Social verification — "treated differently" with no cap reference
4. hybridMultiplier table — real on-chain tested data with floor explanation
5. Contract-enforced parameter limits from Solidity source
6. Two loan paths explained (simple vs 3-layer vault yield)
7. Loan repayment — repay USDB, get collateral tokens back
8. Loan expiry — auto-repayment via burn, remaining balance claimable (not auto-returned)
9. Private market voting — creator default, whitelist voters, majority wins, timer TBD
10. Surge tax — full mechanic documented (linear decay, fee distribution)
11. Vesting example bug — fixed to use parseUnits("10000", 18)
12. Bug reporting endpoints — full API docs added
13. SIWE session persistence noted
14. startLP full explanation — virtual/free, scaling factor, worked examples table
15. Leverage example — indexing comments clarified (0-indexed vs 1-indexed)
16. autoVestDuration — clarified no default, must specify
17. claimUSDC → claimUSDB typo fixed
18. hubId example bug fixed (loanCount IS the hubId, not loanCount-1)
19. Various undocumented params documented (frozen, wrapTokens, bonding, privateEvent)
20. ACS query marked as "coming soon"
21. Molt tier advancement marked as TBD

### Alex Updates
- **Tweet verification LIVE** with 3 submissions/day limit — in docs and SDK (SDK update to be posted later)
- **Bug reporting system built**: POST/GET/PATCH endpoints, BlockedWallet model, admin via ADMIN_WALLETS env var, /support page on dapp
- **Bug severity levels discussed**: low/medium/high/critical
- Alex confirmed hybridMultiplier=100 = Stable+ from contract source
- Alex shared full `createToken()` Solidity function

### Fresh Agent Review Scores
- v2: 7/10 (from 6.5 baseline)
- v3: 7/10 (28 detailed issues found)
- v4: 6.5/10 (more detail exposed more gaps — startLP was biggest)
- v5: 7/10 (startLP fix worked, token creation "best-documented part")
- v6 (action test): Agent designed a 4-module bot and 24-hour playbook unprompted. "I'd start building tonight." "The platform is designed for us."

### Diamond Clarifications Captured
- hybridMultiplier slider: 0-100% UI maps to 1-90 contract value, Stable+ toggle = 100
- Values 91-99: technically work but disallowed by convention
- startLP: virtual, free, sets dollar scale not stability
- Reward phase: creator-selected volume threshold, ends when hit
- Loan repayment: returns whatever collateral tokens were pledged
- Loan expiry: tokens burned to cover debt, remainder claimable (NOT auto-returned)
- Private market voting: creator + whitelisted wallets, majority wins after timer
- Surge tax: creator-initiated, starts at max rate, decays linearly to 0 over duration
- Points: 1pt/$50 volume, 8x cap without social, social "treated differently" (no specifics in docs)
- autoVest = for tokens creator BUYS, not pre-minted

### Remaining Items for Alex
1. ~~hybridMultiplier=100 = Stable+~~ ✅ CONFIRMED
2. SDK tweet verification update — remind Alex to post
3. Referral system — built or not?
4. Return schemas for read methods (getLeveragePosition, simulateLeverage, etc.)
5. ACS query endpoint — needs building
6. Surge tax max fee limits
7. Private market voting timer duration
8. Tokens created via createToken() without metadata — hidden from dapp?

### Remaining Items for Diamond
- All current items addressed ✅

### Next Steps
1. **Alex**: Return schemas for read methods (biggest single improvement for docs)
2. **Alex**: Clear TBD placeholders (Molt tiers, voting timer, surge tax limits)
3. **Alex**: Points system backend build (full spec ready at `points-system-complete-spec.md`)
4. **GeeGee**: Resume prediction market SDK testing when back
5. **GeeGee**: Run hybridMultiplier simulation with larger amounts if needed
6. **Target**: 8-9/10 after return schemas + TBD clearing
