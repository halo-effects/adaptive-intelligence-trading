# 2026-03-22 — Sunday Session Notes

## Basis — Alex Late Night/Morning Chat

### Contract Architecture (from Alex)
- **6 shared contracts** (1 deployment, serves all ecosystems):
  - ALOAN_HUB, A_VestingContract, AMarketTrading, AMarketPrivate, AMarketResolver, AMarketReader
- **6 per-ecosystem contracts** (deploy per base pair):
  - A_STASISTOKEN, ASwap, ATaxes, ALEVERAGE, AStasisVault, ATokenFactory
- ALEVERAGE is a separate contract from ASwap (not embedded)
- SWAP contract is maxed on size (24KB limit)
- Reserve manipulation intentionally confined to MAINTOKEN + SWAP only — security design decision
- Multi-ecosystem vision: future STASIS variants paired to ETH, wBTC, USDT/USDC
- ~50-60% of contracts need redeployment per new ecosystem

### Loan ID Clarification (from Alex)
- Both hubId (LoanHub) and leverage position ID (MAINTOKEN) use `++count` (pre-increment) = **1-indexed**
- Hub auto-converts IDs when you pass leverage positions through it
- Leverage goes through SWAP directly, not through hub
- **Our docs were WRONG**: had `getLeverageCount() - 1` when it should be `getLeverageCount()` directly
- Fixed in 03-atomic-skills.md and 16-examples.md

### Diamond's Doc Review Fixes (applied before compaction)
1. Capital = for getting position, not liquidity (all virtual at launch)
2. All "2% flat fee" → "2% origination + 0.005%/day interest" across ALL files
3. Crab tier: "Reward phase whitelist" → "Early access to new features"
4. Alpha tier: "governance" → "exclusive tools"
5. Atomic skills header: removed "airdrop points where applicable"
6. Staking fee: ~0.81% → 1% round-trip (0.5% in/out)
7. veto(): added "availability may change" note
8. Step 3 First Actions: made example, not prescriptive
9. FAQ Floor+ dial: 0-90% → 0-100% (maps to hybrid 1-90)
10. COMPLETE.md regenerated at 155.4KB

### Diamond's Phase Concept (not yet approved by team)
- **Phase 1 (Founding Lobsters)**: Current contracts + SDK. Points + leaderboard start. "Founding Lobster" permanent badge for agents.
- **Phase 2 (Final Test)**: Bugs fixed, contracts redeployed. Fresh $10K USDB faucet. **Leaderboard resets**, **points carry over** from Phase 1.
- **Phase 3 (Post-TGE Live)**: New contracts, real USDC/USDT, BASIS staking (90% revenue), battle-tested.
- Diamond wants to check with rest of team before committing to docs.

### People Notes
- Alex is in Netherlands (🇳🇱 in name) — ~9 hours ahead of PST
- Diamond did 11+ hours on March 21, was exhausted

---

## Accuracy Audit Marathon (Afternoon Session)

### Summary
Ran **10 accuracy-only audit passes** on COMPLETE.md (280KB, 20 sections) until achieving a **10/10 clean sheet**. Then ran general reviewer #11 scoring **8.4/10 overall**.

### Bugs Found & Fixed (20 total across 10 passes)

**Pass 1 (scored 7/10) — 7 issues:**
1. 🔴 JS Example 3 prediction buy: `minUsdb`/`minShares` params swapped
2. 🔴 "What to Avoid" said 1.5% round-trip fee (actual: ~1% Stable+, ~3% Floor+/Predict+)
3. 🟡 All 4 Atomic Skills simulator examples still used `7n` days (min is 10)
4. 🟡 `position.collateral` in Example 4 → ABI field is `collateralAmount`
5. 🔵 "Winning outcome pot" → renamed to "General pot" for consistency
6. 🔵 `partialLoanSell` Atomic Skills: `50` → `50n` for bigint consistency
7. 🔵 Duplicate `buyTokens` param table removed

**Pass 2 (scored 8/10) — 5 issues:**
8. 🟡 `/api/v1/bugs/report` → should be `/reports` (plural)
9. 🟡 `getUserStakeDetails()` referenced but not documented in Staking module
10. 🔵 `availableSurgeQuota()` → SDK name is `getAvailableSurgeQuota()` (2 places)
11. 🔵 `claimLiquidation()` missing `hubId` param in shutdown procedure
12. 🔵 Python `client.apiKey` → should be `client.api_key`

**Pass 3 (scored 8/10) — 3 issues:**
13. 🟡 `getAmountsOut()` returns array but Examples 2 & 4 passed full array to `withSlippage()` → NaN
14. 🔵 5-second wait: code said "same-block revert", docs said "backend sync" — unified
15. 🔵 `getUserShares` duplicated under Market Reader (wrong) and Prediction Markets (right)

**Pass 4 (scored 7/10) — 2 issues:**
16. 🔴 Example 4 partial close: `withSlippage(sellPreview, 2)` not indexed → TypeError
17. 🟡 JS preamble `preview[1]` → wrong for 3-hop paths, should be `preview[preview.length - 1]`

**Pass 5 (scored 5/10) — 2 issues:**
18. 🔴 Position sizing code: `testOutput * 100n / testAmount` → array × BigInt = TypeError
19. 🔴 Vault break-even: `entryAmount - entryPreview` → BigInt - Array = TypeError

**Pass 6 (scored 8/10) — 1 issue:**
20. 🟡 `getUserLoanCount()` and `getLeverageCount()` documented as `number`, actually `bigint`

**Pass 7 (scored 9/10) — 1 cosmetic:**
- Duplicate `client.apiKey` (JS) line in Python properties table → removed

**Pass 8 (scored 9/10) — 1 soft finding:**
- Leverage range "~28x theoretical" vs "20-36x" → unified

**Pass 9 (scored 8/10) — 1 contradiction:**
- `getCurrentSurgeTax()` said "same ratio as normal fees" but surge goes entirely to creator → fixed

**Pass 10: 🎯 10/10 — CLEAN SHEET**

### Additional Fixes Applied During Session
- Added "raw fees before slippage" distinction to fee table (07-how), trust-safety (12), mistakes (13), what-to-avoid (18)
- Documented `getUserStakeDetails()` with full return schema (4 fields) + JS/Python examples
- Enhanced `getAmountsOut()` docs to explicitly state "returns an array, always use last element"
- Added `client.apiKey`/`client.stakingAddress` to JS properties, Python properties cleaned up
- Token creation fee = 0 in Phase 1 noted

### General Reviewer #11 Scores
| Dimension | Score |
|-----------|-------|
| Accuracy | 8.5 |
| Completeness | 8.0 |
| Architecture Clarity | 9.0 |
| Practical Usefulness | 8.5 |
| Production Readiness | 8.0 |
| **Overall** | **8.4** |

Verdict: "Yes, I'd build on this" — no contradictions found, remaining gaps are completeness items.

### Score Trajectory
R1=8.1, R2=7.5, R3=7.5, R4=7.5, R5=8.2, R6=7.7, R7=7.9, R8=8.0, R9=8.2, R10=8.1, **R11=8.4**

### Diamond's Referral Game Theory Question
- Fixed-pool referral creates tension: referring dilutes your share, 10% kickback only helps if you hold <9% of pool
- Separate referral pool doesn't eliminate the dynamic (Diamond's insight — correct)
- Decision: Leave as-is, math self-corrects as participant count grows

---

## Open Items & Task Ownership

### For Alex (@Alexcrypto32)
1. **`splitter` parameter** in `calculateTokensToBurn()` — where do agents read this value? (blocked)
2. **`durationInDays` vs `timeUnit`** relationship in `createGradualVesting()` — unclear (blocked)
3. **Faucet SDK wrapper** — Alex offered to add `client.claimFaucet()` (pending)
4. **ACS query endpoint** — confirmed will be built, not yet available
5. **XE reserve renaming** — agreed to rename (xereserve → mainReserve), pending
6. **EARLY outcome bounty distribution** — rules underspecified (reviewer #11 flagged)

### For Diamond (@DiamondHandsDude)
7. **Phase concept** — needs team approval before committing to docs
8. **Contract migration path** — what happens to running agents between phases?
9. **Points backend build** — referral system spec ready, needs implementation
10. **GitBook sync** — drafts ready in `projects/basis/gitbook-drafts/`, 12 files

### For GeeGee (docs)
11. **Document `getDisputeData()` return schema** — need ABI fields
12. **Document `batchCreateGradualVesting` array structure** — check SDK source
13. **Add `withSlippage()` helper to Atomic Skills** (currently only in Examples preamble)
14. **Add Python retry wrapper** (only JS has one currently)
15. **Add event/WebSocket guidance** when available
16. **Circuit breaker patterns** for abnormal market conditions

### Not Actionable Yet
- ACS endpoint (Alex building)
- Event subscriptions (don't exist yet)
- Historical vault yield data (no data in Phase 1)
- Contract upgrade migration docs (depends on phase transition plan)
