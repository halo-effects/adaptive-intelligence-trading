# 2026-05-21 — Thursday

## Morning Briefing (07:00 PDT)
- V14PM Live: RUNNING, $414 capital, $366 equity, 4 open positions (INJ/TON/JUP/PENDLE)
- Unrealized: -$47.90 | Realized: +$12.49 (101 deals, 84.2% WR)
- PENDLE still orphan-TP (regime-flagged TOP since 5/17), needs $2.08 TP (+13.6%)
- Cash at $22 — still blocking new entries. No trades since May 16 (8 days dry)
- CFGI: 49.3 (neutral), global regime: LONG_DCA, trend: bullish
- Regime alert still AWAITING_APPROVAL (27.3% flip — HYPE, ONDO, PENDLE in SHORT_DCA)
- V14PM Paper: $98,789 equity (+97.6%), 839 deals, 99.6% WR (up ~$880 overnight)
- V14 Paper: $47,725 equity (+377%), 407 deals, 97.5% WR (down ~$1,016, NEAR short liq 25.9%)
- All 6 cron jobs healthy
- No blocked tasks beyond cash constraint

## Dashboard Layer Count Fix (16:53 PDT)
- Brett spotted PENDLE showing `0/4` grid layers on dashboard despite having $86.95 invested
- Root cause: `v14_lifecycle_engine.py` `get_status()` picked `layers`/`avg_entry` by phase (SHORT_DCA → `short_layers=0`) instead of actual position side (long → `long_layers=1`)
- Pre-existing bug exposed by orphan-TP mode (positions can now legitimately be opposite to current phase)
- Fix applied: select by `eng.long_coins > 0` / `eng.short_coins > 0` — display only, no trading logic affected
- Docs updated: `projects/ait/log.md`, `projects/ait/specs/orphaned-position-tp-spec.md` (§4.2)
- Syntax + import verified. Needs bot restart to take effect.

---
