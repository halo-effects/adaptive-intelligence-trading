# 2026-03-09 Daily Notes

## PM Trade Reconciliation (major)
- Identified 61 trades in CSV: only 20 genuine (18 confirmed + 2 borderline)
- 41 phantom trades from backfill replays during restarts ($943 fake PnL removed)
- Clean runtime window: March 6 08:04 UTC → March 8 18:00 UTC (~58 hours)
- True PM performance: $479.65 realized PnL on $50K capital, 100% win rate, 20 trades

## Fixes Applied
1. **`recorded_at` field** — every trade now has wall-clock timestamp for forensic provenance
2. **`_fresh_floor_ms`** — prevents candle replay on --fresh starts, catches new engines from rebalance too
3. **PID lock (`bot.pid`)** — prevents duplicate instances (scheduled task spawned a 2nd bot that made 24 more phantom trades)
4. **Engine warmup period** — engines wait for first daily boundary before trading (router sets direction first)
5. **V13SignalPack naming note** — added architecture doc note explaining it's a shared signal library, not V13 strategy logic

## Dual Instance Bug
- After cleaning trades.csv to 20, the scheduled task (no --fresh) spawned PID 23356 alongside our clean PID 9220
- Second instance replayed all history in 9 seconds, generating 24 phantom trades
- `recorded_at` field caught it immediately — all 24 had recorded_at within 9 seconds but close_times spanning March 2-9
- Disabled task, re-cleaned CSV, added PID lock, re-enabled task

## Architecture Docs Updated
- V14PM_SYSTEM_ARCHITECTURE.md: §6.5 (warmup), §6.6 (PID lock), §6.7 (recorded_at), V13SignalPack naming note
- Migration docs: noted that PID lock is Windows workaround; cloud uses orchestrator guarantees

## Current Bot State
- PM running PID 9220 with --fresh, 10 engines, waiting for first daily boundary to warm up
- Equity: $50,479.65 (accurate)
- Scheduled task: V14PMPaperBot (re-enabled, PID lock protects against duplication)
- Dashboard synced to GitHub Pages with clean data

## Brett's Key Questions Answered
- "Are 10 engines the coin slots?" → Yes, 10 slots determined by equity tier ($50K), filled by DCA score × trend multiplier
- "What about open trades?" → All 4 were phantom from backfill, safely discarded
- "V13 Signal Pack confusing?" → Yes, it's a shared signal library, rename planned but deferred
- "Fresh floor waits for router?" → Added warmup: engines observe until daily boundary, router sets direction, then trading begins
