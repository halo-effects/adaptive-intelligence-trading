# AIT V14PM — Production Dependencies
# Python 3.12+
#
# These are the ONLY third-party packages required to run the full V14PM system:
# bots, scanner, data pipeline, exchange client, and dashboard sync.
# All other dependencies are Python standard library.
#
# Install: pip install -r requirements.txt

ccxt==4.5.37       # Exchange client — Hyperliquid (perps) + Aster DEX (spot)
numpy==2.4.2       # Numerical computing — signal indicators, DCA grid math
pandas==3.0.0      # Data manipulation — candle DataFrames, signal evaluation
