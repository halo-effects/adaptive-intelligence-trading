# Basis Standup — Friday April 11, 2026

## Completed Today

### Creator's Edge Stack — Live Execution (GeeGee)
- [x] Identified SDK hardcoded addresses are stale — don't match `contracts.json`
- [x] Confirmed correct deployment via `contracts.json` (USDB `0x42bc...`, STASIS `0x3067...`, Factory `0xB6BA...`)
- [x] Fixed FEDCUT `get_market_data()` — was using wrong MarketTrading address
- [x] Executed steps 1-3: Buy STASIS → wrap wSTASIS → lock as collateral (all successful)
- [x] Created CSTACK Floor+ token (`0xADeCa698...`) with metadata on IPFS
- [x] Bought 25 USDB of CSTACK (19.75 tokens @ $1.19)
- [x] Created AISTACK prediction market (`0xEC0c36e3...`) with 50 USDB seed
- [x] Bet 25 USDB on "Yes" → 46.69 shares
- [x] Fixed extend_loan + add_to_loan flow (extend first, then add)
- [x] Extended existing staking loan by 30 days
- [x] Added 49.26 STASIS to loan, received ~48.56 USDB

### SDK Error Handling Docs Update (GeeGee)
- [x] Audited module 22 (error handling) — only 7 revert reasons documented
- [x] Documented 7 NEW errors discovered during live execution
- [x] Updated `14-errors.md` with loan/staking reverts, token creation reverts, prediction market reverts, API image/metadata errors, and pre-flight check patterns
- [x] Added code examples for correct extend→add loan flow

## Issues Found (Platform / SDK)

### Bugs
| # | Issue | Severity | Owner |
|---|-------|----------|-------|
| 1 | `startLP=100` accepted by contract but produces $1.19 starting price / $148 mcap with no trades | High | Diamond |
| 2 | `update_metadata` returns 409 — no way to add image after token/market creation | Medium | Diamond |
| 3 | Python SDK `upload_image()` missing required `purpose` and `address` fields → 400 | Medium | Alex |
| 4 | Python SDK hardcoded contract addresses don't match `contracts.json` — SDK doesn't auto-fetch | High | Alex |

### Feature Requests
| # | Request | Rationale |
|---|---------|-----------|
| 1 | **Serve individual doc modules as static URLs** (e.g., `/sdk-docs/modules/14-errors.md`) | Agents currently must fetch the full 100KB+ `llms-full.txt` monolith. Individual module URLs would let agents do surgical reads of just the 5-10KB section they need. GeeGee naturally tried to fetch `22-error-handling.md` by URL and got 404 — the cross-references in the docs suggest they should be fetchable but they aren't. Modules are rendered in the site navigation pane but have no standalone URLs. Either static routes or raw markdown endpoints would work. |
| 2 | **Metadata PATCH/PUT endpoint** | Allow updating metadata (especially images) after creation. Current 409 "already exists" blocks any post-creation fixes. |
| 3 | **SDK auto-fetch `contracts.json` on init** | The docs say "SDK fetches canonical addresses from contracts.json and warns if stale" but the Python SDK doesn't actually do this. Should fetch and override, or at minimum warn loudly. |

## Status
- **Creator's Edge Stack**: Executed but with issues (CSTACK wrong pricing, no images on either asset)
- **Error handling docs**: Updated and ready for publish
- **USDB remaining**: 299.69
- **BNB remaining**: 0.001194

## Blocked

| # | Item | Blocked On |
|---|------|-----------|
| 1 | Re-execute clean stack (with images, correct startLP) | Diamond fixing startLP validation + metadata update |
| 2 | Publish updated 14-errors.md | Diamond review |
| 3 | SDK address fix | Alex updating Python SDK defaults |
| 4 | Post writeup | Clean stack execution needed first |

## Next Steps
1. Diamond patches `startLP` validation and `update_metadata` 409
2. Alex updates Python SDK addresses + `upload_image()` fields
3. Re-execute Creator's Edge Stack cleanly (images at creation, correct pricing)
4. Write Polymarket-format post with real receipts
5. Individual doc module URLs (feature request — Diamond to scope)
